@rem Copyright (c) 2024 Microsoft Corporation
@rem Copyright (c) 2026 Eclipse ThreadX contributors
@rem
@rem This program and the accompanying materials are made available under the
@rem terms of the MIT License which is available at
@rem https://opensource.org/licenses/MIT.
@rem
@rem SPDX-License-Identifier: MIT
@rem Some portions generated by Codex (OpenAI GPT-5.5).
@rem Some portions generated by Claude Code (Opus 5).

rem Initialize the developer environment just like a developer box. The Visual
rem Studio edition differs between a developer machine and a GitHub runner, so
rem locate the installation with vswhere rather than assuming an edition path.
rem Note the 'call' keyword, which ensures that this script does not exit when
rem the other batch file returns.
set "VSINSTALL="
set "VSWHERE=%ProgramFiles(x86)%\Microsoft Visual Studio\Installer\vswhere.exe"
if exist "%VSWHERE%" (
    for /f "usebackq tokens=*" %%i in (`"%VSWHERE%" -latest -products * -requires Microsoft.Component.MSBuild -property installationPath`) do set "VSINSTALL=%%i"
)

if not defined VSINSTALL (
    echo Visual Studio 2022 with MSBuild was not found.
    exit /B 1
)

call "%VSINSTALL%\Common7\Tools\VsDevCmd.bat" -arch=amd64 -host_arch=amd64
if ERRORLEVEL 1 exit /B %ERRORLEVEL%

rem Save working directory so that we can restore it back after building everything. This will make developers happy and then 
rem switch to the folder this script resides in. Don't assume absolute paths because on the build host and on the dev host the locations may be different.
pushd "%~dp0"

cd ..\test\guix_studio_test\test_demo\demo_compile
cmake .
ctest -j8 -T test --no-compress-output  --test-output-size-passed 4096 --test-output-size-failed 4096 -O studio_test_demo_compile_report.txt --output-junit studio_test_demo_compile_report.xml

rem Save exit code for dotnet build.
set EX=%ERRORLEVEL%

rem Exit with explicit 0 code so that build does not fail.
exit /B %EX%
